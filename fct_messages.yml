version: 2

models:
  - name: fct_messages
    description: Fact table for telegram messages
    columns:
      - name: message_id
        tests:
          - not_null
          - unique
      - name: message_timestamp
        tests:
          - not_null
